#include <stdio.h>
#include <unistd.h>

/**
 * main - print main args without ac
 *
 * Return: Always 0.
 */
int main(int ac, char **av)
{
	int i = 0;

	while (av[i] != '\0')
	{
		printf("%s\n", av[i]);
   		i++;
	}
    return (0);
}
